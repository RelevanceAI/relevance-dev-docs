---
title: "Embed another chain"
---

# Run chain
```python Python
import relevanceai as rai
rai.login()
```
You can now run the chain within a chain!
```python Python
from relevanceai.steps import RunChain

results = RunChain(
    chain_id="...",
    params={...}
).run()
```